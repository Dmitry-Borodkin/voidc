{
    args = v_array_alloca(LLVMTypeRef, 2);
    arg1 = v_getelementptr(args, 1);

    v_store(LLVMTypeRef, args);
    v_store(int,         arg1);

    ft = LLVMFunctionType(LLVMTypeRef, args, 2, 0);

    v_add_symbol_type("LLVMPointerType", ft);
}

{
    char_ptr = LLVMPointerType(char, 0);

    arg = v_alloca(LLVMTypeRef);

    v_store(char_ptr, arg);

    printf_ft = LLVMFunctionType(int, arg, 1, 1);

    v_add_symbol_type("printf", printf_ft);
}
